// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,

};
export var tinyKey:any = "ps4xtippt7h7m3rlnp6dseksfnk3z8kqwlnfj6yhvua3bon9";
// export let baseUrl: any = 'https://pirateportal.herokuapp.com';
export let baseUrl: any = 'localhost:5000';
//export var baseUrl:any = "";
//export var role:any =localStorage.getItem('role');

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
